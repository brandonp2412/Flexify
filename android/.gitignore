gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
# See https://flutter.dev/docs/deployment/android#reference-the-keystore-from-the-app
key.properties
**/*.keystore
**/*.jks

# Deliberately public keystore used ONLY to sign PR test builds
# (com.presley.flexify.pr) so testers can update them in place.
# It must never sign a release. Passwords: pr-testing-public / alias: pr
!app/pr-testing-public.jks
