name: frisbee_flutter_foundation
description: Shared Flutter infrastructure used by Brandon's apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  path_provider: ^2.1.3
