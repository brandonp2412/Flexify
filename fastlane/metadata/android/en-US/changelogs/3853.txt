• ci: auto-build PR APKs only for owner PRs, manual for others
• ci: build and attach APK on pull requests
• ci: fix 0.0.0-unknown Flutter version in PR APK build
• ci: fix quoted artifact URL in PR APK comment
• ci: install PR builds side by side via .pr app id suffix
• ci: sign PR builds with committed public testing keystore
• fix: reset plan set counts at local midnight
• fix: stop rest timer sound/vibrate when a new set starts
• refactor: use AlertDialog for Filters and the shared bottom nav constant
